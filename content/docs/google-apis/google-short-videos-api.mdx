---
title: Google Short Videos API
description: Retrieve real-time short video results from Google Search for fast, scalable SERP data.
---

The Google Short Videos API enables you to retrieve real-time short video results from Google Search, delivering structured data in a simple HTTP request. It is designed for fast, scalable, and reliable access to short-form video SERP data.

<Callout type="warn">
  Requires authentication — provide your API key as a Bearer token in the Authorization header.
</Callout>

`POST` `https://api.serphouse.com/google-short-videos-api`

## Headers

| Name | Type | Description |
|------|------|-------------|
| Authorization | String | Bearer `<YOUR_API_KEY>` |
| Content-Type | String | application/json |

## Request Body

| Name | Type | Description |
|------|------|-------------|
| q | string | Search phrase |
| domain | string | Choose google domain |
| device | string | `desktop` or `mobile` |
| lang | string | Language code e.g. `en`, `fr` |
| loc | string | Optional if specifying `loc_id` |
| loc_id | integer | Optional if specifying `loc` |
| date_range | String | `YYYY-MM-DD,YYYY-MM-DD` or `h`, `d`, `w`, `m`, `y` |
| page | integer | Page number |
| video_quality | String | `high` |
| video_captions | String | `captioned` |

## Example Request

<ApiExample
  endpoint="https://api.serphouse.com/google-short-videos-api"
  query="funny cats"
  params='{"device": "desktop", "loc": "Texas,United States"}'
/>
