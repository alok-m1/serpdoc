---
title: Live (HTTP POST)
description: Perform real-time SERP searches using HTTP POST requests with a JSON body.
---
import { liveHttpPostResponse } from '@/lib/api-examples/liveHttpPost';

<DeprecatedNotice/>

Performing a realtime search using HTTP POST is very simple with the SERPHouse live endpoint.

<Callout type="warn">
  Requires authentication → provide your API key as a Bearer token in the Authorization header.
</Callout>

`POST` `https://api.serphouse.com/serp/live`

### Path Parameters

| Name | Type | Description |
|------|------|-------------|
| responseType | String | `html` or `json` |

### Headers

| Name | Type | Description |
|------|------|-------------|
| Authorization | String | Bearer `<YOUR_API_KEY>` |
| Content-Type | String | application/json |

### Request Body

| Name | Type | Description |
|------|------|-------------|
| q | string | Search phrase |
| domain | string | Google, Bing, or Yahoo domain |
| lang | string | Language code e.g. `en`, `fr` |
| device | string | `desktop` or `mobile` |
| serp_type | string | `web`, `news`, `image`, or `shop` |
| loc | string | Optional if specifying `loc_id` |
| loc_id | integer | Optional if specifying `loc` |
| page | integer | Page number |
| num_result | integer | Results per page |
| date_range | String | Date filter |
| verbatim | integer | Google Verbatim (`0` or `1`) |
| gfilter | integer | Filter on/off (`1` or `0`) |
| no_trace | string \| integer | Enterprise only. Enables NoTrace mode. |

## Example Request

<ApiExample
  endpoint="https://api.serphouse.com/serp/live"
  query="Coffee"
  params='{"device": "desktop", "loc": "New York,United States"}'
/>

## Example Response

<ApiExample mode="response" response={liveHttpPostResponse} />

## Related Links

<Cards>
  <Card title="Live (HTTP GET)" href="/extra-apis/live-http-get" />
  <Card title="Schedule SERP" href="/extra-apis/schedule-serp" />
  <Card title="Google Search API" href="/google-apis/google-search-api" />
</Cards>
